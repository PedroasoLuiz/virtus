node_modules/
.next/
out/
build/

# Segredo nunca entra no repositorio. `.env.example` e a unica versao
# versionada, e ela nao carrega valor nenhum.
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Codigo legado FlutterFlow — referencia de consulta, nunca versionado.
# Contem segredos em claro (token Meta/WhatsApp). Ver docs/06.
_legado/
.vercel

# Documentacao interna: fica no disco, fora do repositorio.
docs/
